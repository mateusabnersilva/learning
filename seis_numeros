soma = 0

for i in range(1, 7): # vai repetir 'DIGITE UM NÚMERO'
    n = int(input('DIGITE UM NÚMERO: '))

    if n % 2 == 0: # valor par será aceito
        soma = soma + n # 0 + NÚMERO DO USUÁRIO
        
print(f'A soma dos números é: {soma}')