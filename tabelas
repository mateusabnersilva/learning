s = ''

while s != 'M' and s != 'F':
    s = str(input('Sexo [M/F]: ')).upper()
    if s != 'M' and s != 'F':
        print('ERRO')

print(f'Você digitou "{s}" ')

# Versão elegante 
'''
s = ''

while s not in 'MF':  
    s = str(input('Sexo[M/F]: '))
    if s not in 'MF':
        print('ERROR')

print(f'Você digitou "{s}" ')


'''